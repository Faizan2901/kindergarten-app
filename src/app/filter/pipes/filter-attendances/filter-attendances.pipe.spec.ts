import { FilterAttendancesPipe } from './filter-attendances.pipe';

describe('FilterAttendancesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAttendancesPipe();
    expect(pipe).toBeTruthy();
  });
});
