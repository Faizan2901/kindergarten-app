import { FilterstudentsPipe } from './filterstudents.pipe';

describe('FilterstudentsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterstudentsPipe();
    expect(pipe).toBeTruthy();
  });
});
